language: php

php:
    - '7.1'
    - '7.2'
    - '7.3'

install: composer install --no-progress
script:
  - pecl install ds
  - vendor/bin/phpstan analyse --no-progress -l 7 -c ./phpstan.neon src tests
  - php -d "zend_extension=xdebug" vendor/bin/phpunit --coverage-text
